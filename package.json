{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "prebuild": "rimraf action/*/dist",
    "build": "lerna exec npm run build",
    "install": "lerna bootstrap"
  },
  "dependencies": {
    "rimraf": "^3.0.2"
  }
}
